---
// ThemeToggle.astro - Client-side island for theme switching
---

<div class="theme-island" id="theme-island">
  <button
    id="theme-toggle-island"
    type="button"
    aria-label="Toggle between light and dark theme"
    aria-pressed="false"
  >
    <span aria-hidden="true" data-theme-icon>🌙</span>
  </button>
</div>

<script is:inline>
  // This script runs on the client side with partial hydration
  // The theme manager is initialized by main.js, this component just provides the UI
  // Theme toggle is handled by the global theme manager
</script>
